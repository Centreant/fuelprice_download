# This is the template for API and database credentials
# Populate the appropriate variables and rename the file as `private.py` then go to `settings.py` and ensure that RUNNING_MODE == 'PRODUCTION'
# If it's a development key then rename to `private_development.py` and set RUNNING_MODE = 'DEVELOPMENT'

# API credentials
API_KEY = ''
API_SECRET = ''

# Database credentials
HOST = ''
DATABASE = ''
USER = ''
PASSWD = ''
AUTH_PLUGIN = ''